{
  "_comment": "Add this to ~/.copilot/mcp.json — Hosted MCP endpoint (no local install needed). The opcua-kb-tools endpoint exposes all 11 tools including the search_docs_rag RAG Q&A tool, so a separate KB-built-in MCP entry is no longer required.",
  "mcpServers": {
    "opcua-kb-tools": {
      "type": "http",
      "url": "https://opcua-kb-sc-mcp-server-v2.ashyglacier-fe56ba7c.swedencentral.azurecontainerapps.io/",
      "headers": {
        "api-key": "REPLACE_WITH_SEARCH_API_KEY"
      }
    }
  }
}
